doctype
html
head
    link(rel='stylesheet', href='http://localhost:8080/styles.css')
    title=name
body
    div.container
        div.answer
            h1=id
            h2=name
            form(action='/poll/' + id + '/vote', method='post', enctype='multipart/form-data')
                ul.poll-options
                    each option in options
                        li.option
                            - optionId = option.option_id
                            - optionValue = option.value

                            input(type='radio', name='vote', value=optionId)
                            label=optionValue
                input(type='submit', value='Vote')



        div.answers
            p=('Total votes: ' + totalVotes)
            p=('Yes: ' + votesFor1)
            p=('Maybe: ' + votesFor3)

            //each vote in votes
                //- voteValue = options[vote_id - 1].value
                //- amountOfVotesForValue = vote
                //p=(optionValue + ': ' + totalVotes)
